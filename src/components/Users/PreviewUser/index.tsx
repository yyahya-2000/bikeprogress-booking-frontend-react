import DrawerContainer from 'components/common/Drawer';
import { FC } from 'react';

const PreviewUserPage: FC = () => {
  return <DrawerContainer>Preview user</DrawerContainer>;
};

export default PreviewUserPage;